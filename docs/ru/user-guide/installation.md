# Справочник по установке

> В этой статье представлен **полный справочник** по способам установки (pip / uv / Docker / устранение неполадок).
> Если вы хотите быстро запустить, [быстрый старт за 5 минут](../quick-start.md) уже охватывает самый простой процесс.

## Требования к системе

- Python 3.10 или новее
- pip или uv (рекомендуется)
- Достаточное свободное место на диске (не менее 100 МБ)

## Способы установки

### Способ 1: Установка с помощью pip

```bash
# Установка ErisPulse
pip install ErisPulse

# Обновление до последней версии
pip install ErisPulse --upgrade
```

### Способ 2: Установка с помощью uv (рекомендуется)

uv — это более быстрая инструментальная цепочка Python, рекомендуемая для среды разработки.

#### Установка uv

```bash
# Установка uv с помощью pip
pip install uv

# Проверка установки
uv --version
```

#### Создание виртуального окружения

```bash
# Создание каталога проекта
mkdir my_bot && cd my_bot

# Установка Python 3.12
uv python install 3.12

# Создание виртуального окружения
uv venv
```

#### Активация виртуального окружения

```bash
# Windows
.venv\Scripts\activate

# Linux/Mac
source .venv/bin/activate
```

#### Установка ErisPulse

```bash
# Установка ErisPulse
uv pip install ErisPulse --upgrade
```

## Инициализация проекта и установка модулей

После завершения установки полный процесс инициализации проекта, установки модулей и запуска описан в разделе [Быстрый старт за 5 минут](../quick-start.md).

### Способ 3: Использование клиентского приложения ErisPulse-App (без терминала)

Не хотите устанавливать среду Python? [ErisPulse-App](../ecosystem/app.md) — это официальное клиентское приложение для всех платформ (Android / Windows / Linux / macOS), которое можно **запускать непосредственно с телефона**, а десктопная версия поддерживает минимизацию в системную панель для фоновой работы. Внутри приложения встроен интерпретатор Python и SDK ErisPulse, поэтому вам не нужно использовать терминал или вручную настраивать параметры:

- Скачайте нужную версию с [GitHub Releases](https://github.com/ErisPulse/ErisPulse-App/releases) в зависимости от вашей платформы (для Android: `online`/`offline` APK, для Windows: `setup.exe`/`zip`, для Linux: `tar.gz`, для macOS: `zip`)
- Создайте и запустите экземпляр в приложении, используя встроенное графическое интерфейс для управления адаптерами и модулями, а также для просмотра каталога модулей

> Подробная инструкция см. в разделе [Установка и использование ErisPulse-App](../ecosystem/app.md).

## Проверка установки

### Проверка установки

```bash
# Проверка версии ErisPulse
epsdk --version
```

### Запуск теста

```bash
# Запуск проекта
epsdk run main.py
```

Если вы увидите подобный вывод, значит установка прошла успешно:

```
[INFO] Инициализация ErisPulse...
[INFO] Загружен адаптер: Yunhu
[INFO] Загружен модуль: MyModule
[INFO] Инициализация ErisPulse завершена
```

## Часто задаваемые вопросы

### Ошибка установки

1. Проверьте, что версия Python >= 3.10 (рекомендуется 3.10 - 3.13)
2. Попробуйте использовать `uv pip install ErisPulse` вместо `pip install`
3. Если появляется ошибка прав, попробуйте `pip install --user ErisPulse` или используйте виртуальное окружение
4. Если возникает ошибка SSL-сертификата в корпоративной сети, попробуйте `pip install --trusted-host pypi.org --trusted-host files.pythonhosted.org ErisPulse`
5. Убедитесь, что сетевое подключение работает, и источник pip доступен

### Ошибка конфигурации

1. Проверьте синтаксис файла `config.toml` (формат TOML чувствителен к отступам и кавычкам)
2. Убедитесь, что все необходимые параметры конфигурации заполнены
3. Просмотрите логи в терминале для получения подробной информации об ошибке
4. Используйте `epsdk init`, чтобы пересоздать файл конфигурации

### Ошибка установки модуля

1. Убедитесь, что имя модуля написано правильно (регистр имеет значение)
2. Проверьте сетевое подключение
3. Используйте `epsdk list-remote`, чтобы просмотреть список доступных модулей
4. Убедитесь, что модуль совместим с текущей версией SDK

### Стратегия выполнения PowerShell в Windows

Если PowerShell выдает сообщение "Невозможно загрузить файл... выполнение скриптов запрещено на этой системе":

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## Далее

- [Справочник команд CLI](cli-reference.md) - Узнайте обо всех командах командной строки
- [Описание конфигурационного файла](configuration.md) - Подробнее о параметрах конфигурации