# Описание конфигурационного файла
> Этот документ будет описывать конфигурационный файл фреймворка. Если у стороннего модуля есть настройки, пожалуйста, обратитесь к документации модуля.

ErisPulse использует конфигурационный файл в формате TOML `config/config.toml` для управления настройками проекта.

## Расположение конфигурационного файла

Конфигурационный файл находится в папке `config/` в корне проекта:

```
project/
├── config/
│   └── config.toml
├── main.py
```

## Обработка ошибок при загрузке конфигурации

Фреймворк при загрузке `config.toml` различает три состояния ошибок и предоставляет **действенные диагностические сообщения**, а не просто возврат к настройкам по умолчанию:

| Состояние ошибки | Условия возникновения | Поведение фреймворка |
|------------------|----------------------|----------------------|
| Файл отсутствует | `config.toml` не существует | При первом запуске нормально, тихо использует пустую конфигурацию (без предупреждений) |
| Ошибка синтаксиса TOML | Файл существует, но формат некорректен (например, отсутствуют кавычки, не закрыты скобки) | Выводит **номер строки/столбца и причину ошибки**, и уведомляет, что была возвращена конфигурация по умолчанию |
| Ошибка прав/другие ошибки | Нет прав на чтение, ошибки ввода-вывода и т.д. | Выводит **ясную причину** и уведомляет, что была возвращена конфигурация по умолчанию |

Например, если вы случайно напишете конфигурацию как `port = 8000` (без кавычек для строки), в логах появится что-то вроде:

```
[ERROR] [Config] Синтаксическая ошибка в конфигурационном файле config/config.toml (строка 3, столбец 1): ...
[WARNING] [Config] Вернулись к конфигурации по умолчанию, ваши пользовательские настройки не применились — пожалуйста, исправьте и перезапустите
```

Таким образом, вы можете сразу определить проблему на **уровне INFO**, а не задаваться вопросом, почему ваши изменения не вступили в силу.

> **Изменение конфигурационного файла во время работы?** Если вы вручную отредактируете `config.toml` во время работы бота и внесёте синтаксическую ошибку, при следующей попытке записи (объединения конфигурации) фреймворк выведет сообщение «Конфигурационный файл повреждён (синтаксическая ошибка, строка X), невозможно объединить и записать — пожалуйста, исправьте конфигурационный файл и перезапустите», а не запутанное «Запись не удалась». Записываемые параметры конфигурации будут сохранены и не потеряются.

## Полный пример конфигурации

```toml
[ErisPulse.server]
host = "0.0.0.0"
port = 8000
ssl_certfile = ""
ssl_keyfile = ""

[ErisPulse.logger]
level = "INFO"
format = "rich"
log_files = []
memory_limit = 1000

[ErisPulse.framework]
enable_lazy_loading = true
uninit_timeout = 30
strict_mode = 0

[ErisPulse.framework.strict_mode_exceptions]
modules = []
adapters = []

[ErisPulse.storage]
use_global_db = false

[ErisPulse.event.command]
prefix = "/"
case_sensitive = true
allow_space_prefix = false
must_at_bot = false

[ErisPulse.event.message]
ignore_self = true

[ErisPulse.i18n]
language = "auto"
```

## Настройки сервера

```toml
[ErisPulse.server]
host = "0.0.0.0"
port = 8000
ssl_certfile = "/path/to/cert.pem"
ssl_keyfile = "/path/to/key.pem"
```

| Настройка | Тип | Значение по умолчанию | Описание |
|-----------|------|------------------------|----------|
| host | string | 0.0.0.0 | Адрес прослушивания, 0.0.0.0 означает все интерфейсы |
| port | integer | 8000 | Порт прослушивания |
| ssl_certfile | string | пустая строка | Путь к файлу SSL-сертификата |
| ssl_keyfile | string | пустая строка | Путь к файлу SSL-ключа |

## Настройки логирования

```toml
[ErisPulse.logger]
level = "INFO"
log_files = ["app.log", "debug.log"]
memory_limit = 1000
```

| Настройка | Тип | Значение по умолчанию | Описание |
|-----------|------|------------------------|----------|
| level | string | INFO | Уровень логирования: TRACE, DEBUG, INFO, WARNING, ERROR, CRITICAL (TRACE — самый низкий уровень, выводит подробную отладочную информацию фреймворка) |
| format | string | rich | Формат вывода логов, по умолчанию используется rich для цветного вывода |
| log_files | array | пустой список | Список файлов для вывода логов |
| memory_limit | integer | 1000 | Количество записей логов, хранящихся в памяти |

## Настройки фреймворка

```toml
[ErisPulse.framework]
enable_lazy_loading = true
uninit_timeout = 30
strict_mode = 0

[ErisPulse.framework.strict_mode_exceptions]
modules = []
adapters = []
```

| Настройка | Тип | Значение по умолчанию | Описание |
|-----------|------|------------------------|----------|
| enable_lazy_loading | boolean | true | Включить ленивую загрузку модулей |
| uninit_timeout | integer | 30 | Общее время ожидания при корректном завершении (секунды), после чего происходит принудительное завершение. 0 означает отсутствие таймаута |
| strict_mode | integer | 0 | Уровень строгого режима, см. описание «Строгий режим» ниже |

### Строгий режим

Строгий режим определяет стратегию обработки компонентов модулей/адаптеров, которые не соответствуют требованиям или не загружаются на этапе загрузки. Современные модули/адаптеры должны наследовать соответствующие базовые классы (`BaseModule`/`BaseAdapter`), компоненты, не наследующие базовые классы, влияют на систему контекста фреймворка и на чистку ресурсов, что может привести к утечке ресурсов.

> **Изменение 2.5.2**: Уровень по умолчанию был изменён с `1` (пропуск) на `0` (допускающий), чтобы снизить количество проблем при первом использовании новыми пользователями. Компоненты, не наследующие базовые классы, будут предупреждены и попытаются загрузиться, а не отклонены напрямую. Для восстановления прежнего поведения, пожалуйста, явно установите `strict_mode = 1`.

| Уровень | Название | Поведение |
|---------|----------|-----------|
| 0 | Допускающий (по умолчанию) | Нарушения только предупреждаются, компоненты, не наследующие базовые классы, всё равно будут пытаться загрузиться (для совместимости со старыми компонентами) |
| 1 | Строгий-пропуск | Отклоняются компоненты, не наследующие базовые классы, и пропускаются, остальное запускается нормально |
| 2 | Строгий-критично | Все нарушения собираются и сообщаются в конце, после чего запуск прерывается |

На всех уровнях, ошибки, возникающие на этапах загрузки/регистрации/инициализации, всегда будут пропущены. Различие заключается в следующем:

- **0 → 1**: Единственное изменение поведения — компоненты, не наследующие базовые классы, теперь пропускаются, а не загружаются.
- **1 → 2**: Все нарушения (не наследование базовых классов, сбой загрузки, сбой регистрации, сбой инициализации и т.д.) становятся критическими, и после сбора всех нарушений на этапе запуска выводится список нарушений и запуск прерывается.

#### Список исключений

Если некоторые компоненты действительно временно не могут быть обновлены (например, из-за зависимостей старых модулей), их можно добавить в список исключений, и компоненты, указанные в списке, будут обрабатываться как допускающие, даже если они не соответствуют требованиям, и будут продолжать загружаться:

```toml
[ErisPulse.framework.strict_mode_exceptions]
modules = ["SeTu", "SomeLegacyModule"]
adapters = ["OldAdapter"]
```

> Когда компонент отклоняется строгим режимом, в логах будет явно указано, как восстановить загрузку (добавив в список исключений или понизив уровень).

## Настройки хранилища

```toml
[ErisPulse.storage]
use_global_db = false
```

| Настройка | Тип | Значение по умолчанию | Описание |
|-----------|------|------------------------|----------|
| use_global_db | boolean | false | Использовать глобальную базу данных (внутри пакета) вместо проектной базы данных. При `true` все проекты используют общую SQLite-базу данных внутри пакета ErisPulse; при `false` (по умолчанию) каждый проект использует отдельную базу данных в папке `config/` |

## Настройки событий

### Настройки команд

```toml
[ErisPulse.event.command]
prefix = "/"
case_sensitive = false
allow_space_prefix = false
```

| Настройка | Тип | Значение по умолчанию | Описание |
|-----------|------|------------------------|----------|
| prefix | string | / | Префикс команды |
| case_sensitive | boolean | true | Регистр чувствителен (например, `/Help` и `/help` — это разные команды) |
| allow_space_prefix | boolean | false | Разрешить пробел в качестве префикса |
| must_at_bot | boolean | false | Команда должна быть вызвана с упоминанием бота (в личных сообщениях это ограничение не действует) |

### Настройки сообщений

```toml
[ErisPulse.event.message]
ignore_self = true
```

| Настройка | Тип | Значение по умолчанию | Описание |
|-----------|------|------------------------|----------|
| ignore_self | boolean | true | Игнорировать собственные сообщения бота |

## Настройки международизации

```toml
[ErisPulse.i18n]
language = "auto"
```

| Настройка | Тип | Значение по умолчанию | Описание |
|-----------|------|------------------------|----------|
| language | string | auto | Язык отображения встроенных текстов фреймворка. При `auto` автоматически определяется системный язык, можно также установить конкретный код: `zh-CN`, `zh-TW`, `en`, `ja`, `ru` |

## Настройки модуля

Каждый модуль может определить свою собственную конфигурацию в конфигурационном файле:

```toml
[MyModule]
api_url = "https://api.example.com"
timeout = 30
enabled = true
```

В модуле можно читать и записывать конфигурацию:

```python
from ErisPulse import sdk

# Чтение конфигурации
config = sdk.config.getConfig("MyModule", {})
api_url = config.get("api_url", "https://default.api.com")

# Запись конфигурации во время выполнения (отложенная сохранение)
sdk.config.setConfig("MyModule.timeout", 60)

# Немедленное сохранение в файл
sdk.config.setConfig("MyModule.timeout", 60, immediate=True)
```

> `setConfig` по умолчанию использует отложенную запись (примерно каждые 5 секунд пакетно сохраняется в файл), установка `immediate=True` немедленно сохраняет. Изменения конфигурации запускают событие жизненного цикла `config.set`.

## Далее

- [Справочник командной строки](cli-reference.md) - узнать о всех командах командной строки
- [Руководство для разработчиков](../developer-guide/) - узнать о разработке пользовательских модулей